/**
 * dsh-edit-resend — Client half types.
 *
 * `lib/client.js` is a UMD side-effect bundle loaded by the DSH client module
 * loader (`window.__ModuleLoader__.load`): it registers the plugin's Remote
 * contribution, injects the turn-tail edit button and the Settings-page
 * restore section, and has no exports of its own. This declaration exists so
 * TypeScript consumers can resolve the `dsh-edit-resend/client` export path.
 */
export {};
